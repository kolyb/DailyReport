@page
@model DailyReport.WebLayer.Pages.Report.IndexModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Reports</h2>
<div>
    <div>
        <a asp-page="/ReportDay/CreateReportDay"
           class="btn btn-info btn-md"
           data-ajax="true"
           data-ajax-update="#reg"
           data-ajax-mode="replace"
           data-ajax-method="get">
            Create report day
        </a>
    </div>
</div>

<div class="container">
    <div class="container-wrapper" style="display:flex; justify-content: space-between; align-items:center">
        <table class="table">
            <thead>
                <tr>
                    <th>Report day</th>
                    <th>*</th>
                    <th>Delete report day</th>
                    <th>*</th>
                    <th>Details plan</th>
                    <th>*</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.ReportDayDTOs is not null)
                {
                    @foreach (var item in Model.ReportDayDTOs)
                    {
                        <tr>
                            <td>@item.RecordDay.ToLongDateString()</td>
                            <td>*</td>
                            <td>
                                <form method="post">
                                    <a asp-page="/ReportDay/DeleteReportDay" asp-route-id="@item.Id">Delete</a>
                                </form>
                            </td>
                            <td>*</td>
                            <td>
                                <form method="post">
                                    <a asp-page="DetailsReport" asp-route-id="@item.Id">Details report</a>
                                </form>
                            </td>
                            <td>*</td>
                        </tr>
                    }
                }
            </tbody>
        </table>
        <div class="main" id="reg" style="align-self:flex-end"></div>
    </div>
</div>

<div>
    <a asp-controller="Home" asp-action="Index">Back</a>
</div>
